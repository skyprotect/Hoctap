# Accessibility Rule

For UI work, target WCAG 2.1 AA where applicable.

Review keyboard navigation, focus visibility/management, semantics, labels, form errors, contrast, text resizing, status messages, and modal behavior.

Prefer native HTML semantics over unnecessary ARIA.
