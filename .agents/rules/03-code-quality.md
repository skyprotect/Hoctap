# Code Quality Rule

Before adding code, search for existing functions, services, components, validators, repositories, APIs, constants, and business rules.

Prefer:
`DELETE → SIMPLIFY → REUSE → CONSOLIDATE → EXTRACT → ADD`

Review complexity, nesting, dependency count, public exports, duplication, side effects, and file growth.
